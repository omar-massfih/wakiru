# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Virtual env / uv
.venv/

# Environment
.env

# Runtime memory store (notes + vector/checkpointer DB) — repo root only,
# so the source package src/assistant/memory/ is still tracked.
/memory/

# OS / editor
.DS_Store
